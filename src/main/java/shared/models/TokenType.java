package shared.models;

public enum TokenType {
    KEYWORD,
    STRING_LITERAL,
    CHAR_LITERAL,
    INTEGER_LITERAL,
    FLOAT_LITERAL,
    IDENTIFIER,
    ETC,
    SEMICOLON,
    EOF,
    OTHER
}
